This is a repository with different linux kernel configs
They are meant to be bare-bones with a minimal subset of functionality.

And yes, they fit on a floppy drive.

Current list:
 - qemu-kvm-* : A kernel suitable for vm's running on kvm.
                Assumes you're providing virtio, virtnet and kvm tsc from host
